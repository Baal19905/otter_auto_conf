1. create_tables.sh 根据tables里内容自动创建src_id的表
2. create_mapping.sh 根据tables里内容自动创建pipeline_id的映射关系
3. delete_tables.sh 删除src_id下的所有表
4. delete_mapping.sh 删除pipeline_id下的所有映射关系

注意：
1. 使用以上脚本需要先建好库源、pipeline
2. tables内容为db.table，table可以是*
3. tables内容可以使用#注释